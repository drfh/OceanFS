//
//
//
// salt mup -d
// salt get --cluster cluster
// salt get --swarm UUID --
//
// salt --config filename.json read --node UUID --block SHA256 > test_file.test
// salt --config filename.json write --node UUID --block SHA256 < test_file.test
//
// salt mup
// salt mup -d
//
//
// salt --config [fs_path|BASE64|BASE96] status
// salt --config [fs_path|BASE64|BASE96] status --cluster [UUID]
// salt --config [fs_path|BASE64|BASE96] status --cluster [UUID] --json
// salt --config [path;] status [--cluster;--store;--pool;--block] [UUID] --json pretty -d

// salt --config status --store [UUID]
// salt --config status --pool [UUID]
//
//
// salt --config status
// salt --config status
// salt --config status
//
//
//
//
//
//
